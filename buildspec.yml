version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  #install:
    #commands:
      # - command
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
      echo "this is test 30/01"
      # - command
  #post_build:
    #commands:
      # - command
      # - command
artifacts:
  files:
     - /var/local/entrypoint.sh
    # - location
  discard-paths: yes
  #base-directory: location
